name: BBA local checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  tests:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        python: ["3.10", "3.12"]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - run: python -m pip install --upgrade pip build
      - run: python -m pip install -e .
      - run: python -m compileall -q bba tests
      - run: python -m unittest discover -s tests -p 'test_*.py' -v
      - run: git diff --check
      - run: python -m build

  sandbox-macos:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: python -m pip install -e .
      - run: bba sandbox-status
      - run: python -m unittest tests.test_security -v

  sandbox-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: sudo apt-get update
      - run: sudo apt-get install --yes bubblewrap
      - run: python -m pip install -e .
      - run: bba sandbox-status
      - run: python -m unittest tests.test_security -v
